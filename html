<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anyango Victoria | Senior Legal Counsel</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --navy: #0a192f;
            --light-navy: #112240;
            --slate: #8892b0;
            --light-slate: #a8b2d1;
            --white: #e6f1ff;
            --accent: #64ffda;
            --accent-tint: rgba(100, 255, 218, 0.1);
            --gold: #f4d03f;
            --transition: all 0.4s cubic-bezier(0.215, 0.61, 0.355, 1);
            --section-padding: clamp(5rem, 10vw, 8rem) 0;
            --border-radius: 12px;
            --shadow: 0 10px 30px -15px rgba(2, 12, 27, 0.7);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--slate);
            background-color: var(--navy);
            overflow-x: hidden;
        }

        h1, h2, h3, h4, .logo {
            font-family: 'Cormorant Garamond', serif;
            color: var(--white);
            font-weight: 600;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        section {
            padding: var(--section-padding);
            position: relative;
        }

        /* === Animated Background Elements === */
        .bg-shape {
            position: absolute;
            border-radius: 50%;
            filter: blur(40px);
            opacity: 0.05;
            z-index: -1;
        }
        .shape-1 { width: 300px; height: 300px; background: var(--accent); top: 10%; right: -100px; animation: float 25s infinite ease-in-out; }
        .shape-2 { width: 200px; height: 200px; background: var(--gold); bottom: 20%; left: -50px; animation: float 20s infinite ease-in-out reverse; }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-40px) rotate(180deg); }
        }

        /* === Enhanced Navigation === */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            padding: 1.5rem 0;
            transition: var(--transition);
            background-color: rgba(10, 25, 47, 0.9);
            backdrop-filter: blur(10px);
        }
        header.scrolled { padding: 1rem 0; box-shadow: var(--shadow); }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-size: 2rem;
            text-decoration: none;
            font-weight: 700;
            letter-spacing: 1px;
        }
        .logo span { color: var(--accent); }
        .nav-links { display: flex; list-style: none; gap: 2.5rem; }
        .nav-links a {
            color: var(--light-slate);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            padding: 0.5rem 0;
            position: relative;
            transition: var(--transition);
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent);
            transition: width 0.4s ease;
        }
        .nav-links a:hover { color: var(--white); }
        .nav-links a:hover::after { width: 100%; }

        .mobile-menu-btn { display: none; font-size: 1.5rem; background: none; border: none; color: var(--accent); cursor: pointer; }

        /* === Cinematic Hero Section === */
        .hero {
            min-height: 105vh;
            display: flex;
            align-items: center;
            padding-top: 100px;
            position: relative;
            overflow: hidden;
        }
        .hero-content { 
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }
        .hero-text { max-width: 600px; }
        .hero-greeting {
            color: var(--accent);
            font-family: monospace;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            display: block;
            opacity: 0;
            animation: fadeUp 0.8s 0.2s forwards;
        }
        .hero-title {
            font-size: clamp(2.8rem, 5vw, 4.5rem);
            line-height: 1.1;
            margin-bottom: 1.5rem;
            opacity: 0;
            animation: fadeUp 0.8s 0.4s forwards;
        }
        .hero-title span { color: var(--accent); position: relative; }
        .hero-desc {
            font-size: 1.25rem;
            color: var(--light-slate);
            margin-bottom: 2.5rem;
            opacity: 0;
            animation: fadeUp 0.8s 0.6s forwards;
        }
        .hero-image-container {
            position: relative;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--shadow);
            opacity: 0;
            transform: translateX(30px);
            animation: slideInRight 1s 0.8s forwards;
        }
        .hero-image {
            width: 100%;
            height: auto;
            display: block;
            transition: transform 1s ease;
        }
        .hero-image-container:hover .hero-image { transform: scale(1.03); }
        
        .btn {
            display: inline-block;
            background: transparent;
            color: var(--accent);
            padding: 1rem 2rem;
            border: 1px solid var(--accent);
            border-radius: var(--border-radius);
            text-decoration: none;
            font-weight: 500;
            font-size: 1rem;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            z-index: 1;
            opacity: 0;
            animation: fadeUp 0.8s 0.8s forwards;
        }
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--accent-tint);
            transition: left 0.5s ease;
            z-index: -1;
        }
        .btn:hover::before { left: 0; }
        .btn:hover { color: var(--navy); background: var(--accent); transform: translateY(-3px); }
        
        .btn-primary {
            background: var(--accent);
            color: var(--navy);
            margin-right: 1rem;
        }
        .btn-primary:hover { background: transparent; color: var(--accent); }

        /* === Animations === */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes slideInRight {
            to { opacity: 1; transform: translateX(0); }
        }

        /* === About Section === */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }
        .about-text h2 { font-size: 2.5rem; margin-bottom: 1.5rem; }
        .about-text h2 span { color: var(--accent); }
        .about-text p { margin-bottom: 1.5rem; font-size: 1.1rem; }
        .about-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            margin-top: 3rem;
        }
        .stat-item { text-align: center; }
        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--accent);
            font-family: 'Cormorant Garamond', serif;
            display: block;
            line-height: 1;
        }
        .stat-label {
            font-size: 0.9rem;
            color: var(--slate);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 0.5rem;
        }

        /* === Credentials Section === */
        .credentials {
            background: linear-gradient(180deg, var(--navy) 0%, var(--light-navy) 100%);
        }
        .section-title {
            text-align: center;
            margin-bottom: 4rem;
        }
        .section-title h2 {
            font-size: 2.8rem;
            margin-bottom: 1rem;
        }
        .section-title p {
            color: var(--slate);
            max-width: 600px;
            margin: 0 auto;
            font-size: 1.1rem;
        }
        .certificates-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        .certificate-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(100, 255, 218, 0.1);
            border-radius: var(--border-radius);
            padding: 2rem;
            text-align: center;
            transition: var(--transition);
            opacity: 0;
            transform: translateY(30px);
        }
        .certificate-card.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .certificate-card:hover {
            transform: translateY(-10px);
            border-color: var(--accent);
            box-shadow: var(--shadow);
        }
        .certificate-icon {
            font-size: 2.5rem;
            color: var(--accent);
            margin-bottom: 1.5rem;
        }
        .certificate-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--white);
        }
        .certificate-card p {
            color: var(--slate);
            margin-bottom: 1.5rem;
        }

        /* === Professional Experience Timeline === */
        .timeline {
            position: relative;
            max-width: 800px;
            margin: 0 auto;
        }
        .timeline::before {
            content: '';
            position: absolute;
            left: 50%;
            top: 0;
            bottom: 0;
            width: 2px;
            background: linear-gradient(to bottom, transparent, var(--accent), transparent);
            transform: translateX(-50%);
        }
        .timeline-item {
            margin-bottom: 3rem;
            position: relative;
            width: 45%;
            padding: 2rem;
            background: rgba(255, 255, 255, 0.03);
            border-radius: var(--border-radius);
            border: 1px solid rgba(100, 255, 218, 0.1);
            transition: var(--transition);
            opacity: 0;
            transform: translateY(30px);
        }
        .timeline-item.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .timeline-item:nth-child(odd) { margin-left: 55%; }
        .timeline-item:nth-child(even) { margin-left: 0; }
        .timeline-item:hover {
            border-color: var(--accent);
            transform: translateY(-5px);
            box-shadow: var(--shadow);
        }
        .timeline-date {
            color: var(--accent);
            font-weight: 600;
            margin-bottom: 0.5rem;
            display: block;
        }
        .timeline-content h3 {
            font-size: 1.4rem;
            margin-bottom: 0.5rem;
            color: var(--white);
        }
        .timeline-content h4 {
            color: var(--slate);
            font-weight: 500;
            margin-bottom: 1rem;
        }
        .timeline-content ul {
            list-style: none;
            padding-left: 1rem;
        }
        .timeline-content li {
            margin-bottom: 0.5rem;
            position: relative;
            padding-left: 1.5rem;
        }
        .timeline-content li::before {
            content: '▸';
            position: absolute;
            left: 0;
            color: var(--accent);
        }

        /* === Skills Section === */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        .skill-category {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(100, 255, 218, 0.1);
            border-radius: var(--border-radius);
            padding: 2.5rem;
            transition: var(--transition);
            opacity: 0;
            transform: translateY(30px);
        }
        .skill-category.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .skill-category:hover {
            transform: translateY(-10px);
            border-color: var(--accent);
            box-shadow: var(--shadow);
        }
        .skill-category h3 {
            display: flex;
            align-items: center;
            gap: 1rem;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            color: var(--white);
        }
        .skill-category h3 i { color: var(--accent); }
        .skill-list { list-style: none; }
        .skill-list li {
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .skill-name { display: flex; align-items: center; gap: 0.8rem; }
        .skill-name i { color: var(--accent); font-size: 0.8rem; }
        .skill-level { color: var(--gold); font-weight: 600; }

        /* === Contact Section === */
        .contact-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
        }
        .contact-info h3 { font-size: 2rem; margin-bottom: 2rem; }
        .contact-item {
            display: flex;
            align-items: flex-start;
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        .contact-icon {
            width: 50px;
            height: 50px;
            background: var(--accent-tint);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent);
            font-size: 1.2rem;
            flex-shrink: 0;
        }
        .contact-details h4 { color: var(--white); margin-bottom: 0.3rem; }
        
        .contact-form .form-group { margin-bottom: 1.5rem; }
        .contact-form label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--light-slate);
            font-weight: 500;
        }
        .contact-form input,
        .contact-form textarea {
            width: 100%;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(100, 255, 218, 0.2);
            border-radius: var(--border-radius);
            color: var(--white);
            font-family: 'Inter', sans-serif;
            transition: var(--transition);
        }
        .contact-form input:focus,
        .contact-form textarea:focus {
            outline: none;
            border-color: var(--accent);
            background: rgba(100, 255, 218, 0.05);
        }

        /* === Footer === */
        footer {
            background: var(--light-navy);
            padding: 4rem 0 2rem;
            text-align: center;
        }
        .footer-content {
            max-width: 800px;
            margin: 0 auto;
        }
        .social-links {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        .social-link {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.05);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--light-slate);
            font-size: 1.2rem;
            transition: var(--transition);
            text-decoration: none;
        }
        .social-link:hover {
            background: var(--accent);
            color: var(--navy);
            transform: translateY(-5px);
        }
        .copyright {
            color: var(--slate);
            font-size: 0.9rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* === Responsive Design === */
        @media (max-width: 992px) {
            .hero-content,
            .about-content,
            .contact-content {
                grid-template-columns: 1fr;
                gap: 3rem;
            }
            .hero-text { order: 2; }
            .hero-image-container { order: 1; max-width: 500px; margin: 0 auto; }
            .timeline::before { left: 30px; }
            .timeline-item { width: calc(100% - 80px); margin-left: 80px !important; }
        }

        @media (max-width: 768px) {
            .mobile-menu-btn { display: block; }
            .nav-links {
                position: fixed;
                top: 0;
                right: -100%;
                width: 280px;
                height: 100vh;
                background: rgba(10, 25, 47, 0.95);
                flex-direction: column;
                padding: 100px 40px;
                transition: right 0.5s ease;
                backdrop-filter: blur(10px);
            }
            .nav-links.active { right: 0; }
            .nav-links li { margin: 1rem 0; }
            .hero-title { font-size: 2.5rem; }
            .section-title h2 { font-size: 2.2rem; }
            .about-stats { grid-template-columns: repeat(2, 1fr); }
        }

        @media (max-width: 576px) {
            .about-stats { grid-template-columns: 1fr; }
            .certificates-grid { grid-template-columns: 1fr; }
            .skills-grid { grid-template-columns: 1fr; }
            .btn { display: block; width: 100%; margin-bottom: 1rem; }
            .btn-primary { margin-right: 0; }
        }
    </style>
</head>
<body>
    <!-- Animated Background Shapes -->
    <div class="bg-shape shape-1"></div>
    <div class="bg-shape shape-2"></div>

    <!-- Navigation -->
    <header id="header">
        <div class="container">
            <nav class="navbar">
                <a href="#" class="logo">A.<span>Victoria</span></a>
                
                <button class="mobile-menu-btn" id="mobileMenuBtn">
                    <i class="fas fa-bars"></i>
                </button>
                
                <ul class="nav-links" id="navLinks">
                    <li><a href="#home">Home</a></li>
                    <li><a href="#about">About</a></li>
                    <li><a href="#credentials">Credentials</a></li>
                    <li><a href="#experience">Experience</a></li>
                    <li><a href="#skills">Skills</a></li>
                    <li><a href="#contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <span class="hero-greeting">Senior Legal Counsel & Advocate</span>
                    <h1 class="hero-title">Anyango <span>Victoria</span></h1>
                    <p class="hero-desc">
                        A distinguished legal professional specializing in constitutional law, civil litigation, 
                        and corporate advisory. Committed to delivering strategic legal solutions with integrity 
                        and excellence.
                    </p>
                    <div class="hero-actions">
                        <a href="#contact" class="btn btn-primary">Schedule Consultation</a>
                        <a href="#experience" class="btn">View Experience</a>
                    </div>
                </div>
                
                <div class="hero-image-container">
                    <!-- Your professional portrait -->
                    <img src="https://i.postimg.cc/F1fM6bqM/Whats-App-Image-2025-12-30-at-09-53-37.jpg" 
                         alt="Anyango Victoria - Professional Portrait" class="hero-image">
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <div class="container">
            <div class="about-content">
                <div class="about-text">
                    <h2>About <span>Me</span></h2>
                    <p>
                        I am a dedicated legal practitioner with extensive experience in both public and private 
                        sectors. My practice spans constitutional matters, civil litigation, corporate advisory, 
                        and land law disputes, with a proven track record of successful case resolutions.
                    </p>
                    <p>
                        Currently pursuing a Post Graduate Diploma in Legal Practice at the Law Development 
                        Center, Kampala, I bring comprehensive understanding of Ugandan law coupled with 
                        strategic litigation expertise and a client-centered approach.
                    </p>
                    
                    <div class="about-stats">
                        <div class="stat-item">
                            <span class="stat-number">50+</span>
                            <span class="stat-label">Cases Handled</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number">100%</span>
                            <span class="stat-label">Client Satisfaction</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number">4+</span>
                            <span class="stat-label">Years Experience</span>
                        </div>
                    </div>
                </div>
                
                <div class="about-image">
                    <!-- Optional: Could add another image here if available -->
                </div>
            </div>
        </div>
    </section>

    <!-- Credentials & Certifications Section -->
    <section class="credentials" id="credentials">
        <div class="container">
            <div class="section-title">
                <h2>Credentials & Certifications</h2>
                <p>Professional qualifications and certifications demonstrating legal expertise</p>
            </div>
            
            <div class="certificates-grid">
                <div class="certificate-card">
                    <div class="certificate-icon">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <h3>Bachelor of Laws (LLB) Hons</h3>
                    <p>Uganda Christian University, Kampala</p>
                    <!-- Certificate Image 1 -->
                    <img src="https://i.postimg.cc/WtDBymKx/Whats-App-Image-2025-12-30-at-10-02-00.jpg" 
                         alt="LLB Certificate" style="width:100%; border-radius:8px; margin-top:1rem;">
                </div>
                
                <div class="certificate-card">
                    <div class="certificate-icon">
                        <i class="fas fa-award"></i>
                    </div>
                    <h3>Post Graduate Diploma</h3>
                    <p>Law Development Center, Kampala (In Progress)</p>
                    <!-- Certificate Image 2 -->
                    <img src="https://i.postimg.cc/0r6T4G3L/Whats-App-Image-2025-12-30-at-10-04-05.jpg" 
                         alt="Post Graduate Diploma" style="width:100%; border-radius:8px; margin-top:1rem;">
                </div>
                
                <div class="certificate-card">
                    <div class="certificate-icon">
                        <i class="fas fa-certificate"></i>
                    </div>
                    <h3>Professional Memberships</h3>
                    <p>FIDA Uganda • Uganda Law Society • Red Cross Society</p>
                    <div style="margin-top:1rem; padding:1rem; background:rgba(100,255,218,0.05); border-radius:8px;">
                        <p style="font-size:0.9rem; color:var(--accent); margin-bottom:0.5rem;">
                            <i class="fas fa-check-circle"></i> Mentor - SheLaws Program
                        </p>
                        <p style="font-size:0.9rem; color:var(--accent);">
                            <i class="fas fa-check-circle"></i> Member - FIDA Uganda
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Experience Section -->
    <section id="experience">
        <div class="container">
            <div class="section-title">
                <h2>Professional Experience</h2>
                <p>A timeline of my legal career and key achievements</p>
            </div>
            
            <div class="timeline">
                <div class="timeline-item">
                    <span class="timeline-date">May - August 2025</span>
                    <div class="timeline-content">
                        <h3>Clerkship</h3>
                        <h4>Ministry of Justice and Constitutional Affairs</h4>
                        <ul>
                            <li>Conducted targeted legal research across statutes and case law</li>
                            <li>Drafted comprehensive legal opinions and defense statements</li>
                            <li>Observed and documented high-profile courtroom proceedings</li>
                            <li>Assisted in developing litigation strategies for complex cases</li>
                        </ul>
                    </div>
                </div>
                
                <div class="timeline-item">
                    <span class="timeline-date">January - September 2024</span>
                    <div class="timeline-content">
                        <h3>Legal Assistant</h3>
                        <h4>Ascend & Co Advocates, Kampala</h4>
                        <ul>
                            <li>Redacted confidential information from sensitive legal documents</li>
                            <li>Conducted client interviews and streamlined case management</li>
                            <li>Researched legal precedents supporting complex attorney cases</li>
                            <li>Assisted in trial preparation and deposition coordination</li>
                        </ul>
                    </div>
                </div>
                
                <div class="timeline-item">
                    <span class="timeline-date">June - October 2023</span>
                    <div class="timeline-content">
                        <h3>Legal Intern</h3>
                        <h4>Ministry of Internal Affairs</h4>
                        <ul>
                            <li>Verified legal documents and applications</li>
                            <li>Reviewed and summarized complex case files</li>
                            <li>Assisted in immigration law compliance monitoring</li>
                            <li>Prepared legal briefs for senior counsel review</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="skills">
        <div class="container">
            <div class="section-title">
                <h2>Professional Skills</h2>
                <p>Comprehensive legal expertise and technical competencies</p>
            </div>
            
            <div class="skills-grid">
                <div class="skill-category">
                    <h3><i class="fas fa-gavel"></i> Litigation Excellence</h3>
                    <ul class="skill-list">
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Civil Litigation</span>
                            </div>
                            <span class="skill-level">Expert</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Constitutional Law</span>
                            </div>
                            <span class="skill-level">Advanced</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Corporate Law</span>
                            </div>
                            <span class="skill-level">Advanced</span>
                        </li>
                    </ul>
                </div>
                
                <div class="skill-category">
                    <h3><i class="fas fa-file-contract"></i> Legal Operations</h3>
                    <ul class="skill-list">
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Legal Research</span>
                            </div>
                            <span class="skill-level">Expert</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Document Drafting</span>
                            </div>
                            <span class="skill-level">Expert</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Case Management</span>
                            </div>
                            <span class="skill-level">Advanced</span>
                        </li>
                    </ul>
                </div>
                
                <div class="skill-category">
                    <h3><i class="fas fa-language"></i> Languages & Technical</h3>
                    <ul class="skill-list">
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>English</span>
                            </div>
                            <span class="skill-level">100%</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Luganda</span>
                            </div>
                            <span class="skill-level">100%</span>
                        </li>
                        <li>
                            <div class="skill-name">
                                <i class="fas fa-chevron-right"></i>
                                <span>Samia</span>
                            </div>
                            <span class="skill-level">60%</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <div class="container">
            <div class="section-title">
                <h2>Contact Me</h2>
                <p>Get in touch for legal consultation and representation</p>
            </div>
            
            <div class="contact-content">
                <div class="contact-info">
                    <h3>Let's Discuss Your Legal Needs</h3>
                    <p style="margin-bottom: 2rem;">
                        I'm available for consultations and legal representation. Reach out to discuss 
                        how I can assist with your legal matters.
                    </p>
                    
                    <div class="contact-item">
                        <div class="contact-icon">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="contact-details">
                            <h4>Location</h4>
                            <p>Kampala Central, Uganda</p>
                        </div>
                    </div>
                    
                    <div class="contact-item">
                        <div class="contact-icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div class="contact-details">
                            <h4>Phone</h4>
                            <p>+256 753 078851</p>
                            <p>+256 775 263959</p>
                        </div>
                    </div>
                    
                    <div class="contact-item">
                        <div class="contact-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="contact-details">
                            <h4>Email</h4>
                            <p>anyangovictorial@gmail.com</p>
                        </div>
                    </div>
                </div>
                
                <div class="contact-form">
                    <form id="consultationForm">
                        <div class="form-group">
                            <label for="name">Full Name</label>
                            <input type="text" id="name" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="email">Email Address</label>
                            <input type="email" id="email" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="subject">Subject</label>
                            <input type="text" id="subject" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="message">Message</label>
                            <textarea id="message" rows="5" required></textarea>
                        </div>
                        
                        <button type="submit" class="btn btn-primary" style="width: 100%;">
                            Send Message
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <h2 class="logo" style="font-size: 2.5rem; margin-bottom: 1.5rem;">Anyango Victoria</h2>
                <p style="max-width: 600px; margin: 0 auto 2rem;">
                    Senior Legal Counsel specializing in constitutional law, civil litigation, 
                    and corporate legal advisory services.
                </p>
                
                <div class="social-links">
                    <a href="#" class="social-link">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="mailto:anyangovictorial@gmail.com" class="social-link">
                        <i class="fas fa-envelope"></i>
                    </a>
                    <a href="tel:+256753078851" class="social-link">
                        <i class="fas fa-phone"></i>
                    </a>
                    <a href="#" class="social-link">
                        <i class="fas fa-file-pdf"></i>
                    </a>
                </div>
                
                <div class="copyright">
                    <p>&copy; 2025 Anyango Victoria. All rights reserved. | Legal Counsel & Advocate</p>
                    <p style="margin-top: 0.5rem; font-size: 0.8rem;">
                        Member: Uganda Law Society • FIDA Uganda • Red Cross Society Uganda
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Mobile Menu Toggle
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const navLinks = document.getElementById('navLinks');
        
        mobileMenuBtn.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            mobileMenuBtn.innerHTML = navLinks.classList.contains('active') 
                ? '<i class="fas fa-times"></i>' 
                : '<i class="fas fa-bars"></i>';
        });

        // Header Scroll Effect
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
            
            // Update active nav link
            updateActiveNavLink();
            
            // Trigger animations
            triggerAnimations();
        });

        // Update Active Nav Link
        function updateActiveNavLink() {
            const sections = document.querySelectorAll('section');
            const navLinks = document.querySelectorAll('.nav-links a');
            
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                
                if (scrollY >= (sectionTop - 200)) {
                    current = section.getAttribute('id');
                }
            });
            
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${current}`) {
                    link.classList.add('active');
                }
            });
        }

        // Scroll Animations
        function triggerAnimations() {
            const animateElements = document.querySelectorAll('.certificate-card, .timeline-item, .skill-category');
            
            animateElements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                
                if (elementTop < windowHeight - 100) {
                    element.classList.add('visible');
                }
            });
        }

        // Form Submission
        const contactForm = document.getElementById('consultationForm');
        contactForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            
            // Show success message
            alert(`Thank you ${name}! Your consultation request has been sent. I will respond to you at ${email} within 24 hours.`);
            
            // Reset form
            contactForm.reset();
        });

        // Close mobile menu when clicking a link
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
                mobileMenuBtn.innerHTML = '<i class="fas fa-bars"></i>';
            });
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Initialize animations on load
        window.addEventListener('load', () => {
            triggerAnimations();
        });
    </script>
</body>
</html>
